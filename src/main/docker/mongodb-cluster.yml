version: '2'
services:
    crmadmin-mongodb:
        image: mongo:3.2.10
        ports:
            - "27017:27017"
        command: mongos --configdb crmadmin-mongodb-config
    crmadmin-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    crmadmin-mongodb-config:
        image: mongo:3.2.10
        container_name: crmadmin-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
